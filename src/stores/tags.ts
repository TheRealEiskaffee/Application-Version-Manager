import { defineStore } from 'pinia';

export const useTagsStore = defineStore('tags', {
    state: () => ({
        tags : new Map(),
    }),

    getters: {
        
    },

    actions: {
        
    }
});
